# Publish ONLY the detailed map + the option chooser. Ignore everything else so
# nothing personal (GPX tracks, notes, accommodation, CLAUDE.md, settings) is ever committed.
*
!.gitignore
!Norja26_KarttaDetailed.html
!index.html
!Norja26_Reitti.html
